
export default function Topbar() {
  return (
    <div className="fixed top-0 left-0 w-full h-16 bg-blue-950 text-white flex items-center justify-between px-6 shadow-md z-50">
      {/* Left section */}
      <div className="flex items-center space-x-3">
        <h1 className="text-lg font-semibold">menu</h1>
        <h1 className="text-lg font-semibold">menu</h1>
        <h1 className="text-lg font-semibold">menu</h1>
        <h1 className="text-lg font-semibold">menu</h1>
      </div>
    </div>
  );
}
